<?xml version="1.0" encoding="utf-8"?>
<!-- White page behind the ô-ly handwriting grid (NotebookView draws the grid on top). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/km_notebook_bg" />
    <corners android:radius="@dimen/km_radius_sm" />
    <stroke
        android:width="1dp"
        android:color="@color/km_bubble_assistant_border" />
</shape>
