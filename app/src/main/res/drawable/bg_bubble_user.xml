<?xml version="1.0" encoding="utf-8"?>
<!-- User (child) speech bubble — right aligned, grass-green, tail on bottom-right. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/km_bubble_user" />
    <corners
        android:topLeftRadius="@dimen/km_radius_lg"
        android:topRightRadius="@dimen/km_radius_lg"
        android:bottomRightRadius="@dimen/km_radius_bubble_tail"
        android:bottomLeftRadius="@dimen/km_radius_lg" />
</shape>
