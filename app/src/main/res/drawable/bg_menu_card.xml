<?xml version="1.0" encoding="utf-8"?>
<!-- Friendly rounded card behind the hamburger popup menu. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/km_radius_lg" />
    <solid android:color="@color/km_menu_card_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/km_menu_divider" />
</shape>
